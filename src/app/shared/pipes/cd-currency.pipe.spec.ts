import { CdCurrencyPipe } from './cd-currency.pipe';

describe('CdCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new CdCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
