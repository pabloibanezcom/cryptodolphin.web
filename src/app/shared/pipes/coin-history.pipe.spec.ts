import { CoinHistoryPipe } from './coin-history.pipe';

describe('CoinHistoryPipe', () => {
  it('create an instance', () => {
    const pipe = new CoinHistoryPipe();
    expect(pipe).toBeTruthy();
  });
});
