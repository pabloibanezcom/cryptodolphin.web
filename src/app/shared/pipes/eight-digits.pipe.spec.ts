import { EightDigitsPipe } from './eight-digits.pipe';

describe('EightDigitsPipe', () => {
  it('create an instance', () => {
    const pipe = new EightDigitsPipe();
    expect(pipe).toBeTruthy();
  });
});
