import { CurrencySelectorPipe } from './currency-selector.pipe';

describe('CurrencySelectorPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencySelectorPipe();
    expect(pipe).toBeTruthy();
  });
});
